/*
Objects Demo

Data to use in demo (paste below steps and uncomment):

const user = {
  name_first: "Jackie",
  name_last: "Levine",
  phone: "7789188724",
  email: "jackie.levine@fullstackacademy.com"
}

const cart = ["tee-shirt", "tee-shirt", "pants", "scarf", "scarf", "scarf"];

const students = [
  {
    name_first: "Jess",
    name_last: "Duell"
  },
  {
    name_first: "Caitlin",
    name_last: "Lamprecht"
  },
  {
    name_first: "Doobin",
    name_last: "Lee"
  }
]

👉 STEP 1: Create a function stringifyObject that turns an object into a stringified version.
Test with example user object.

👉 STEP 2: Create a function jsonifyString that turns a stringified object into a JSON object.
Test with stringified object returned from function created in step 1.

👉 STEP 3: Create two functions logValuesOne and logValuesTwo that log the values of an object.
Test with example user object.

👉 STEP 4: Create two functions logKeysOne and logKeysTwo that log the keys of an object.
Test with example user object.

👉 STEP 5: Create a function mapOrder that creates an object based on the order array above
tracking the amount of each item in the order, like so:
{
  tee-shirt: 2,
  pants: 1,
  scarf: 3
}

👉 STEP 6: Create a function addKeyValuePair that takes an object, a key, and a value, and returns
a copy of the object with the new key/value pair added.

The key must be a string, and the value can be any type.

Use the sample user object and the key username, and the value "jroyankfan42"

👉 STEP 7: Create a function getFirstNames that takes the students array and returns an array of just the student first names.
*/
